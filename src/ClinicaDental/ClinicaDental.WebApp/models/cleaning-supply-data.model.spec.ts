import { CleaningSupplyData } from './cleaning-supply-data.model';

describe('CleaningSupplyData', () => {
  it('should create an instance', () => {
    expect(new CleaningSupplyData()).toBeTruthy();
  });
});
