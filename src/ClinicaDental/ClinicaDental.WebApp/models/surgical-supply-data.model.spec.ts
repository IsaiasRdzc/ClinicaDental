import { SurgicalSupplyData } from './surgical-supply-data.model';

describe('SurgicalSupplyData', () => {
  it('should create an instance', () => {
    expect(new SurgicalSupplyData()).toBeTruthy();
  });
});
